"""categories 도메인 서비스(비즈니스 로직).

여기에 넣을 것:
- router에서 호출할 함수들
  - create_*, update_*, delete_*, list_*, get_*
- DB 접근(Tortoise 쿼리) + 검증/권한체크 + 외부연동 호출(integrations)

규칙:
- router.py에는 로직을 최소화하고, 실제 처리는 service로 내려보내기.
"""
