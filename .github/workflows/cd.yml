# name: Development Server Deployment

# on:
#   pull_request:
#     type: ['closed']
#     branches:
#     - develop

# jobs:
#   dev_deploy:
#     # 머지가 된 후 close된 pr에만 작동.
#     if: github.event.pull_request.merged == true
#     runs-on: ubuntu-24.04
#     steps:
#       - name: Checkout Repository # docker build . 을 할 깃허브 레포 코드를 가져옴.
#         uses: actions/checkout@v3

#       - name: Login to DockerHub # 도커 로그인
#         user: docker/login-action@v2
#         with:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}

#       - name: Set up Docker Buildx #! 이게 뭐 임?
#         user: docker/setup-buildx-action@v3

#       - name: Build and Push Docker image # 이미지 빌드 푸시
#         run: |
#           docker build --platform linux/amd64 -t $